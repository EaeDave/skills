---
name: test-picker
description: Temporary skill used only to verify that the skills installer shows the skill-selection TUI when a repository has more than one skill. Remove before publishing a production skill catalog.
---

# Test Picker

Use this skill only as an installation test.

When invoked, respond briefly that the skill was installed successfully and that `test-picker` is temporary and should be removed before publishing the final skills repository.
